<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyProject"
        tools:targetApi="31">

        <!-- 기존 MainActivity에서 intent-filter 제거 -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Join을 메인 화면으로 설정 -->
        <activity
            android:name=".Join"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Gym"/>
        <activity android:name=".Pila"/>
        <activity android:name=".Swim"/>
        <activity android:name=".Tennis"/>
        <activity android:name=".Personal"/>
        <activity android:name=".BaseActivity"/>
        <activity android:name=".Gym_detail1"/>
        <activity android:name=".Pila_detail"/>
        <activity android:name=".Swim_detail"/>
        <activity android:name=".Tennis_detail"/>
        <activity android:name=".CardCharge"/>
        <activity android:name=".JoinDetail"/>
        <activity android:name=".SearchResults"/>
        <activity android:name=".Mission"/>
    </application>
</manifest>
